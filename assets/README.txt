Drop optional art here to upgrade the visuals:

  leesin.png   — Lee Sin splash art, shown large in the hero (transparent PNG
                 works best). Without it, a soft energy glow is shown instead.
  photo.jpg    — the photo inside the polaroid on the love card.

Both are optional — the experience runs without them. Nothing is uploaded;
these are read locally by your browser.
